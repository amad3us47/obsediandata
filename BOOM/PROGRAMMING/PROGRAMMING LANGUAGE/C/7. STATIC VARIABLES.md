
They are like global variables.

```c
#include<stdio.h>
int main()
{
     static int x;
     printf("%d",x);
     return 0;
}
```

Uninitialized static variables have 0 value.
Static local variables are initialized only once and stay throughout the program's life.
Static Global Variables have internal linkage.