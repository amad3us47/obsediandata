
```c
#include<stdio.h>
int main()
{             // uninitialized
    int x=10; //local varibale
    printf("%d",x);
    return 0;
}
```

Output:
Undefined



```c
#include<stdio.h>  // uninitialized
int x=10;  //global variable
int main()
{
    printf("%d",x);
    return 0;
}
```

Output:
0

```c
#include<stdio.h>
extern int x;
int main()
{
    printf("%d",x);
    return 0;
}
int x=10;
```

Output:
10


What happens when we have local and global variables with same name?

```c
#include<stdio.h>
int x=20;
int main()
{
    int x=10;
    printf("%d",x);
    return 0;
}
```

Output:
10

What happens when we have same variables name in multiple scopes?

```c
#include<stdio.h>
int x=20;
int main(0
{
   int x=10;
   {
      int x=30;
      printf("%d",x);
   }
   return 0;
}
```

Output:
30

