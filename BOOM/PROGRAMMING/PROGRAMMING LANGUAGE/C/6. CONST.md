It makes the variable constant.

```c
#include<stdio.h>
int main(0
{
    const int x =10;
    x=x+1;
    printf("%d",x);
    return 0;
}
```

Output:
compiler error

```c
#include <stdio.h>
const double PI=3.14;
int main()
{
    int r;
    printf("Enter value of r:\n");
    scanf("%d",&r;)
    printf("Area IS %f",PI*r*r);
    return 0;
}
```

Alternative to const ( #define )

```C
#include <stdio.h>
int main()
{
   int r;
   printf("Enter value of r:\n");
   scanf("%d",&r;)
   printf("Area IS %f",PI*r*r);
   return 0;
   
}
```