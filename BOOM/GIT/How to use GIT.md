```
set git to default
git config --global rerere.enable
```


Configure git to global 
```
git config --add --global user.name "amad3us47"
git config --add --global user.email "markus39107@gmail.com"
```

Creating a new Repo
```
git init
```
Output : Initialized empty Git repository in /home/ubuntu/Desktop/GIT/.git/


Basic git usage
```
git addd <path-to-file> | pattern >
git commit -m "mesage"
git status
```
Output : [master (root-commit) a813261] ddd        // the first commit


History of commit 
```
git log
```

It has all the changes 
```
.git/objects
```